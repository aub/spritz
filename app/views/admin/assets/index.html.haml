.box
  .boxheader
    %span.header= assets_name
    = link_to 'Add New', new_admin_asset_path
  .asset-list.boxcontent
    - @assets.each do |asset|
      %span
        = render :partial => 'asset', :object => asset
