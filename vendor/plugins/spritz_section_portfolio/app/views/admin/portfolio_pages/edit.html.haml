= error_messages_for :portfolio_page
                                   
- form_for :portfolio_page, :url => admin_portfolio_section_portfolio_page_path(@section, @portfolio_page), :html => { :method => :put } do |f|
  = render :partial => 'form', :locals => { :f => f }

%br
= render :partial => 'portfolio_page'